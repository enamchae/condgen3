{
  "name": "condgen-backtracking",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run parcel\" \"npm run bsync\"",
    "parcel": "parcel watch src/index.html",
    "bsync": "browser-sync start -s \"dist\" -f \"dist\"",
    "build": "parcel build src/index.html"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.0.0",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "browser-sync": "^2.27.7",
    "parcel-bundler": "^1.12.5"
  }
}
